{
  "name": "FilesystemSinkSpec",
  "description": "FilesystemSinkSpec configures filesystem output.",
  "fields": [
    {
      "name": "Path",
      "yamlKey": "path",
      "type": "string",
      "required": false,
      "template": false,
      "description": "Path is the directory to write files to (default: current directory).",
      "enum": null,
      "ref": null,
      "default": null
    },
    {
      "name": "Prefix",
      "yamlKey": "prefix",
      "type": "string",
      "required": false,
      "template": true,
      "description": "Prefix is prepended to filenames.",
      "enum": null,
      "ref": null,
      "default": null
    }
  ]
}
