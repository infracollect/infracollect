{
  "name": "ArchiveSpec",
  "description": "ArchiveSpec configures bundling output into an archive.",
  "fields": [
    {
      "name": "Format",
      "yamlKey": "format",
      "type": "string",
      "required": true,
      "template": false,
      "description": "Format is the archive format. Currently only \"tar\" is supported.",
      "enum": [
        "tar"
      ],
      "ref": null,
      "default": null
    },
    {
      "name": "Compression",
      "yamlKey": "compression",
      "type": "string",
      "required": false,
      "template": false,
      "description": "Compression algorithm",
      "enum": [
        "gzip",
        "zstd",
        "none"
      ],
      "ref": null,
      "default": null
    },
    {
      "name": "Name",
      "yamlKey": "name",
      "type": "string",
      "required": false,
      "template": true,
      "description": "Name is the archive base name, defaults to the job name.\nThe appropriate file extension (e.g., \".tar.gz\") is automatically appended.",
      "enum": null,
      "ref": null,
      "default": null
    }
  ]
}
