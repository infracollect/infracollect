{
  "name": "S3Credentials",
  "description": "S3Credentials provides explicit S3 credentials.",
  "fields": [
    {
      "name": "AccessKeyID",
      "yamlKey": "access_key_id",
      "type": "string",
      "required": true,
      "description": "AccessKeyID is the AWS access key ID.",
      "enum": null,
      "ref": null,
      "default": null
    },
    {
      "name": "SecretAccessKey",
      "yamlKey": "secret_access_key",
      "type": "string",
      "required": true,
      "description": "SecretAccessKey is the AWS secret access key.",
      "enum": null,
      "ref": null,
      "default": null
    }
  ]
}
